# ============================================
# DOCKER COMPOSE ENVIRONMENT
# ============================================

# Database
POSTGRES_DB=whatsapp_store
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here_change_this

# Redis (if password protected)
# REDIS_PASSWORD=your_redis_password
